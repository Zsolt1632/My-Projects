apply plugin: 'application'

dependencies {
    implementation project(':mzim2273-backend') // Depend on the backend module
    implementation 'org.jdatepicker:jdatepicker:1.3.4'
}

application {
    // Specify the main class for the desktop application
    mainClassName = 'edu.bbte.idde.mzim2273.Main'
}
